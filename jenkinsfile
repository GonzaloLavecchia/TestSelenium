node('master') {
    stage('Checkout') {
        checkout scm
    }

    stage('Run tests') {
        withMaven(maven: 'Maven 3') {
            dir('Selenium') {
                sh 'mvn clean test -Dwebdriver.type=chrome'
            }
        }
    }
}